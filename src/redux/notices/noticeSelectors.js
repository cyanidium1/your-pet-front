export const selectAllNotices = state => state.notices.allNotices;
export const selectSelectedNotice = state => state.notices.selectedNotice;
export const selectIsNoticesLoading = state => state.notices.isLoading;
export const selectFilterOptions = state => state.notices.filterOption;
